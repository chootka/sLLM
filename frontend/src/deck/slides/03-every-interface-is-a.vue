<template>
<div class="content">
<h2>Every interface is a filter</h2>
<p class="lead">Seven prompts, one interface. Each changes only what the model is told it
is holding.</p>
<div class="prompts"><div><p class="label">BLIND</p><p>The interface and nothing else. Never told what it is coupled to.</p></div><div><p class="label">INFORMED</p><p>Same interface, plus the organism's name.</p></div><div><p class="label">CYCLES</p><p>The period is withheld. Every duration it sets uses its own estimate.</p></div><div><p class="label">ADVERSARIAL</p><p>The organism's activity consumes the model's context. Blue light quiets the organism.</p></div><div><p class="label">METERED</p><p>The organism's period sets the memory budget. Shorter period, more context.</p></div><div><p class="label">MIMIC</p><p>No history. A decaying trail on the surface instead. Changes, not values.</p></div><div><p class="label">NULL</p><p>BLIND with the task removed. A claim of influence there comes from the format.</p></div></div>
</div>
<figure class="media">
<img src="/deck-media/the-organism-massed-around-the-central.jpg" alt="The organism massed around the central island, 6 September">
</figure>
</template>

<script>
export default {
  name: '03_every_interface_is_a',
  meta: {
    classes: 'split content-wide',
    section: 'The question',
    reg: 0.55, fx: 50, fy: 52
  }
}
</script>
